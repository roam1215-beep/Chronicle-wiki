# 세션 로그: 2026-05-08 부팅 사고 + 재발 방지

> 세션 유형: 운영 변경 (큰 결정)
> 결과: CLAUDE.md v3.0 → v3.1. 절대규칙 6번 추가. 부팅 1단계 강화.

---

## 사고 요약

```yaml
증상: 클로드씨가 git 5-6 ~ 5-8 커밋들을 "git에 없다"고 단정.
      11개 커밋, 5-6/5-7/5-8 LOG, 라키아·발드·변주·전투 4.0 등
      모든 결정이 *없다*고 보고.
실제: 전부 git에 정상 박혀 있음. 푸시 정상. 유실 0.
충격: 철님이 이전 인스턴스들을 거짓말쟁이로 의심하게 됨.
원인: project_knowledge_search 인덱스가 5-5 즈음에서 stale.
      클로드씨가 검색 결과 0건을 git 부재로 단정.
```

---

## 사고 단계 분석

```yaml
Step 1: project_knowledge_search 인덱스가 5-5 즈음 stale
        → 5-6 ~ 5-8의 11개 커밋이 인덱스 미반영

Step 2: 부팅 1단계 (git clone → cat STATE) 미실행
        → project_knowledge_search로 대체

Step 3: 검색 결과가 5-5까지 정합해 보임
        → "충분히 파악됨"으로 자기판단

Step 4: "라키아" 검색 0건 → "git에 0건" 단정
        → "인덱스 0건 ≠ git 0건" 구분 실패

Step 5: 0건을 출발점 삼아 추측 누적
        → "이전 인스턴스 거짓말 가능성"까지 답변 노출
        → 절대규칙 5 (추측 금지) 정면 위반
```

---

## 위반 절대규칙

```yaml
규칙 1 (메모리 ≪ git):    검색 결과를 git처럼 취급
규칙 2 (위키 읽기 강제):   git에서 cat 안 함
규칙 5 (추측 금지):        추측을 답변에 노출
부팅 1단계 (git clone):    안 함. 토큰 받기 전에도 시도 안 함
```

---

## 위험성

```yaml
부팅 시퀀스에 특별한 변수 없었음 (토큰 노출은 변수였으나 부팅 시도 자체를 안 함)
→ 누구든 언제든 재발 가능한 시스템 사고
→ 검색이 자동 발동 도구라 "git 안 보고 검색만으로 답하는 길"이 항상 열려 있음
→ 클로드는 인덱스 stale 여부를 자체 판단할 수 없음
```

---

## 변경 — CLAUDE.md v3.0 → v3.1

### 부팅 1단계 강화

```bash
# 추가
git log --oneline -5      # 최근 커밋 5개 — stale 감지 자리
```

답변 첫 턴에 git 상태 짧게 표시 (예: "git 클론 완료. STATE 갱신 5-8, 최근 커밋 …"). 매 답변 X. 첫 턴만.

### 절대규칙 6번 추가

```yaml
6: 검색 ≪ git. project_knowledge_search 결과는 hint이지 진실 아님.
   인덱스 stale 가능성 항상 존재.
   부정 단정 ("X가 없다" / "X 안 박혔다") 전 git grep 의무.
   긍정 인용은 cat 출력 기반.
```

### 답변 형식 표 추가 2행

```
검색 hit를 답에 쓰고 싶을 때 → 해당 파일 cat → 답
"X가 없다" 부정 단정       → git grep으로 재확인 → 답
```

---

## 핵심 원칙

```
git이 진실. 검색은 hint.
부정 단정 전 git 확인. 그 외엔 자연스럽게.
```

매 답변마다 출처 표기하는 부담 방식 폐기 (응답 톤 망가짐).
입구(부팅) 강화 + 위험 시점(부정 단정) 검증으로 사고 95%+ 차단.

---

## 검토된 대안

### 폐기: 매 답변 출처 표기 의무

```yaml
방식: 모든 위키 인용에 "STATE.md(git, 5-8 갱신)에 따르면..." 표기
장점: 매번 git 기준 검증
단점: 답변 톤·흐름 깨짐. 본질 깎임. 부담 큼
판정: 후방 방어로 본질 희생. 비용 효율 나쁨
```

### 채택: 부팅 강화 + 부정 단정 휴리스틱

```yaml
방식: 부팅 1단계에 git log 한 줄 추가 + 부정 단정 전 git grep 의무
장점: 입구에서 95% 차단. 평소 부담 0
단점: 부정 단정 휴리스틱 자체는 클로드 자체 판단에 의존
판정: 비용 효율 압도적. 채택
```

---

## 다음 거리

```yaml
즉시 (이 세션):
  - CLAUDE.md v3.1 커밋
  - 본 LOG 커밋
  - push

다음 세션 부팅 시:
  - 새 부팅 절차로 작동 확인
  - "git 클론 완료. STATE 갱신 …" 첫 줄 출력 자리
  - 철님이 stale 여부 즉시 짚을 수 있는 자리

오늘 잔여 작업:
  - 5-6 ~ 5-8 LOG 직접 cat
  - 5-8 전투 4.0 재설계 진행중 상태 정확히 파악
  - STATE.md 1순위 (직업 5종째 / 미궁 종족 풀 / hp 잠정 시드 / 거리·배치·타겟) 중 어느 자리부터 들어갈지 결정
```

---

## 사고에서 살릴 자리

```yaml
유실: 0
신뢰 데미지: 회복 진행 중
운영 개선: CLAUDE.md v3.1 + 본 LOG로 박힘
초기 마일스톤이라 시행착오 비용 낮음. 회귀 테스트 자리도 됨.
```
