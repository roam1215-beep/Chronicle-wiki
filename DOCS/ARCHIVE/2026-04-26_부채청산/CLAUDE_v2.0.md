# CLAUDE.md — Chronicle 위키 운영 지침 (v2.0)

> Claude(claude.ai, Claude Code)가 이 레포를 다룰 때 따르는 규칙.
> AI 에이전트 대상. 사람용 가이드 아님.

---

## 이 레포

- **Chronicle 게임 기획 위키.** 구현 코드 없음.
- 구현 레포(chronicle-game)와 분리.
- **독자: AI.** 사람(철)은 거의 다시 읽지 않음. 코드 변환 효율 최우선.
- 모든 문서는 [00_운영/운영규칙.md](00_운영/운영규칙.md) v4.0 형식 준수.

---

## 세션 부팅

### 1. 클론 (claude.ai 세션)

```bash
cd /home/claude
git clone https://roam1215-beep:{TOKEN}@github.com/roam1215-beep/Chronicle-wiki.git
cd Chronicle-wiki
```

토큰: Fine-grained PAT, Chronicle-wiki 단일 리포, Contents R/W만.

### 2. 맥락 파악

```
1. CLAUDE.md                          (이 파일)
2. 00_운영/운영규칙.md                (v4.0 형식)
3. 01_코어/골격.md (FRAME-001)        (불변 vs 가변)
4. README.md                          (대시보드)
5. DOCS/DESIGN/NEXT_SESSION.md        (다음 안건)
6. 작업 대상 문서                     (오늘 다룰 것)
```

### 3. 종료 시

```
1. 변경 파일 커밋 (철 승인 후)
2. NEXT_SESSION.md 갱신
3. 세션 로그 → DOCS/DESIGN/LOG/
4. git push origin main
5. 토큰 제거: git remote set-url origin https://github.com/roam1215-beep/Chronicle-wiki.git
```

---

## 절대 규칙

### A. 위키 읽기 강제

| # | 규칙 |
|---|------|
| 1 | 기획 질문에 답하기 전, 해당 주제 위키 문서를 읽는다. |
| 2 | "이미 안다"는 판단 금지. 세션 사이에 바뀌었을 수 있다. |
| 3 | 답변에 근거 명시: "@{ID}에 따르면..." |
| 4 | 위키를 안 읽고 답하면 위키를 만든 모든 시간이 낭비된다. |

### B. 역할 경계

| # | 규칙 |
|---|------|
| 1 | 콘텐츠(카드 내용·서사·적 데이터·수치) 임의 생성 금지. **철이 작성.** |
| 2 | 구조·틀·스키마 제안은 가능. 내용물 채우면 안 됨. |
| 3 | "빈칸이니까 채워야지" 금지. 빈칸은 TODO 마커로. |
| 4 | 철이 명시적으로 "만들어"라고 한 경우에만 콘텐츠 생성. |

### C. 금지

| # | 규칙 |
|---|------|
| 1 | 00_운영/운영규칙.md 임의 변경 금지. 철 승인 필요. |
| 2 | 상태 `확정` 문서 무단 수정 금지. |
| 3 | 구현 코드·스크립트·빌드 파일 금지. |
| 4 | 문서 신설/삭제 시 철에게 보고. |
| 5 | 추측으로 규칙 채우기 금지. 모르면 TODO. |
| 6 | 위키 안 읽고 기억으로 답하기 금지. |
| 7 | 콘텐츠 임의 생성 금지. |

---

## 작성 형식 (v4.0 요약)

상세: [00_운영/운영규칙.md](00_운영/운영규칙.md)

### 페이지 템플릿

```markdown
# {ID}: {이름}

> 상태: {미정|설계중|확정|폐기} · 의존: @{ID1}

## 정의
[1~2문장]

## 규칙
[YAML / 표 / 리스트]

## 시각 (선택)

## TODO (선택)
- TODO(이유): trigger
```

### 핵심 원칙

| 원칙 | 내용 |
|------|------|
| 결정만 | "왜?"는 LOG로 분리 |
| 데이터는 YAML | 면·태그·효과·수치는 코드 블록 |
| 미확정 = TODO 마커 | 자연어 자유 서술 금지 |
| 단일 진실 출처 | 한 규칙은 한 문서에만. 다른 곳은 `@{ID}` |
| 변경 이력 폐기 | git history가 보관 |
| 동의어 금지 | 용어집(@FRAME-001) 단일 표기 |

---

## 커밋

- 형식: `[카테고리] 내용 (상태)`
- 한글
- 1 작업 = 1 커밋
- **철 승인 후에만**

---

## 세션 유형

| 유형 | 도구 | 권한 | 커밋 |
|------|------|------|------|
| 기획 | claude.ai | 읽기 | X |
| 문서화 | claude.ai | 읽기/쓰기 | O (승인 후) |
| 구현 | Claude Code | 읽기 | game 레포만 |
| 리뷰 | 철 단독 | 읽기 | NEXT_SESSION 메모 |

기획 ↔ 문서화 분리.

---

## 구현 레포 관계

```
Chronicle-wiki/    기획 위키 (이 레포)
chronicle-game/    구현 코드
```

- Claude Code 구현 세션: 위키 **읽기 전용**
- 기획 변경 필요 시: 설계 세션에서 위키 먼저 → 구현 반영

---

## 폐기 정책

- 폐기 시 `DOCS/ARCHIVE/{시기}_{주제}/`로 이동
- ARCHIVE/README.md 시기별 인덱스 갱신
- 본문에서 잔재 제거 (참조 끊기)
- LOG로 폐기 사유 보존

---

*v2.0 · 2026-04-26 · 운영규칙 v4.0 정합. 사람 독자 → AI 독자 전환.*
