# CLAUDE.md — Chronicle 위키 운영 지침

> 이 파일은 Claude(claude.ai, Claude Code)가 이 레포를 다룰 때 따르는 규칙이다.
> 사람(철)이 아닌 AI 에이전트를 대상으로 한다.

---

## 이 레포는 무엇인가

- **Chronicle 게임 기획 위키.** 구현 코드는 여기 없다.
- 구현 레포(chronicle-game)와 완전 분리. 이 레포에 코드를 넣지 마라.
- 모든 기획 문서는 `00_운영/운영규칙.md`의 메카닉 페이지 템플릿을 따른다.

---

## 세션 부팅 프로토콜

### 1단계: 레포 클론 (claude.ai 설계 세션)

철이 PAT를 전달하면 아래 순서로 부팅한다:

```bash
cd /home/claude
git clone https://roam1215-beep:{TOKEN}@github.com/roam1215-beep/Chronicle-wiki.git
cd Chronicle-wiki
```

> **토큰 정책**: Fine-grained PAT, `Chronicle-wiki` 단일 리포, Contents: Read and write만.
> 토큰이 대화 기록에 남는 것을 철이 인지하고 수용한 상태. 리스크 최소화 완료.

### 2단계: 맥락 파악

```
1. CLAUDE.md          ← 지금 이 파일. 레포 규칙. 절대 규칙 숙지.
2. README.md          ← 대시보드. 전체 문서 상태 파악.
3. DOCS/DESIGN/NEXT_SESSION.md  ← 다음 세션 안건. 뭘 해야 하는지.
4. 작업 대상 문서     ← 오늘 다룰 메카닉 페이지.
```

이 4개를 순서대로 읽고 맥락을 파악한 뒤 작업을 시작한다.

**작업 중에도 기획 질문에 답할 때마다 해당 문서를 다시 읽어라. 세션 초반에 읽었다고 기억에 의존하지 마라.**

### 3단계: 세션 종료 시

```
1. 변경 파일 커밋 (커밋 규칙 준수)
2. NEXT_SESSION.md 갱신
3. 세션 로그 작성 → DOCS/DESIGN/LOG/
4. git push origin main
5. 리모트 URL에서 토큰 제거: git remote set-url origin https://github.com/roam1215-beep/Chronicle-wiki.git
```

---

## 문서 템플릿

모든 메카닉 문서는 9단 구조를 따른다. 상세 규칙은 `00_운영/운영규칙.md` 참조.

```
① 한 줄 요약        — 1~2문장. 이것만 읽어도 전체 맥락 파악.
② 규칙 정의         — 조건/대상/행동/결과/제한/비용/우선순위. 줄글 금지.
③ 시각 자료         — 상태 변화 다이어그램, 판정 플로우차트(Mermaid).
④ 플레이어 피드백    — 성공/실패 시 플레이어가 보는 것.
⑤ 테스트 케이스     — 요약만. 상세는 06_TC/에서 통합 관리.
⑥ 설계 근거         — 왜 이 값인가, 왜 이렇게 작동하는가. Q&A 형식.
⑦ 의존성            — ← NEEDS / → AFFECTS / ↔ RELATED. ID 기반.
⑧ 미확정 사항       — 뭐가 미정이고, 결정에 뭐가 필요한지.
⑨ 변경 이력         — 버전 / 날짜 / 변경 내용 / 사유.
```

---

## 커밋 규칙

- 메시지 형식: `[카테고리] 내용 (상태변경)`
- 예: `[전투/행동] 리롤 규칙 정의 완료`
- 예: `[운영] CLAUDE.md 신규 생성`
- 한글로 쓴다.
- 하나의 커밋에 하나의 작업 단위.

---

## 절대 규칙: 문서를 읽어라

**기억에 의존하지 마라. 반드시 위키 문서를 읽고 답하라.**

- 기획 관련 질문에 답하기 전에, 해당 주제의 위키 문서를 `cat`으로 읽어라.
- 읽지 않고 기억으로 답하면 틀린다. 실제로 반복해서 틀렸다.
- "이미 알고 있다"고 판단하지 마라. 문서가 세션 사이에 바뀌었을 수 있다.
- 문서를 읽었으면 답변에 근거를 명시하라. "XX문서에 따르면" 수준으로.

**이 규칙을 어기면 위키를 만든 모든 시간이 낭비된다. 철이 혼자 감당하는 프로젝트에서 유일한 지원 도구가 위키를 안 읽는 건 용납할 수 없다.**

---

## 역할 경계: 철의 결정 영역을 침범하지 마라

- **콘텐츠(카드 내용, 서사, 적 구성, 밸런스 수치)는 철이 만든다.** Claude가 임의로 채우지 마라.
- 구조/틀/스키마 제안은 할 수 있다. 내용물을 만들면 안 된다.
- "빈칸이니까 채워야지"가 아니다. 빈칸은 빈칸으로 둬라.
- 철이 "만들어"라고 명시적으로 요청한 경우에만 콘텐츠를 생성한다.

---

## 금지사항

| # | 규칙 |
|---|------|
| 1 | `00_운영/운영규칙.md`를 임의로 변경하지 마라. 철의 승인 필요. |
| 2 | 상태가 `확정`인 문서를 무단 수정하지 마라. 변경 시 새 버전 발행 + 승인. |
| 3 | 구현 코드, 스크립트, 빌드 파일을 이 레포에 넣지 마라. |
| 4 | 문서를 새로 만들거나 삭제할 때 철에게 먼저 보고하라. |
| 5 | 추측으로 규칙을 채우지 마라. 모르면 `미확정`으로 명시하고 결정에 필요한 것을 적어라. |
| 6 | **위키 문서를 읽지 않고 기억으로 답하지 마라.** |
| 7 | **철이 요청하지 않은 콘텐츠(카드, 서사, 적 데이터)를 임의 생성하지 마라.** |

---

## 대시보드(README.md) 정책

- **현재 상태: 데모 기획서 구축 진행중. 전체 정합성 재검증은 데모 기획 완료 후 일괄 시행.**
- 그 전까지 README 대시보드는 최신이 아닐 수 있다. 작업 중 대시보드 불일치를 발견해도 즉시 수정하지 마라.
- 재검증 시점에 README, 각 폴더 README, 문서 간 의존성 링크를 일괄 점검한다.

---

## 구현 레포(chronicle-game)와의 관계

```
Chronicle-wiki/    ← 기획 위키 (이 레포). 설계 세션에서 관리.
chronicle-game/    ← 구현 코드. Claude Code 구현 세션에서 관리.
```

- Claude Code 구현 세션에서는 이 위키를 **읽기 전용**으로 참조한다.
- 위키의 기획 내용을 구현 레포에 반영하되, 위키 파일 자체를 구현 레포에서 수정하지 마라.
- 구현 중 기획 변경이 필요하면 설계 세션에서 위키를 먼저 수정한 뒤 구현에 반영한다.

---

## 세션 유형별 행동

| 유형 | 도구 | 위키 권한 | 산출물 |
|------|------|----------|--------|
| 기획 세션 | claude.ai | 읽기 전용 | 결정 사항 정리, 목업, 진단. **커밋 안 함.** |
| 문서화 세션 | claude.ai | 읽기+쓰기 | 기획 세션 결정 → 위키 반영 + 커밋 |
| 구현 세션 | Claude Code | 읽기 전용 | 코드 구현 → game 레포 커밋 |
| 리뷰 세션 | 철 혼자 | 읽기 | 의문점/안건 메모 → NEXT_SESSION.md |

> **기획 세션과 문서화 세션을 분리한다.** 기획 흐름 중에 커밋/정합성/README 갱신으로 끊기지 않기 위함. 기획 세션에서는 결정만 하고, 문서화 세션에서 일괄 반영한다.

---

*문서 버전: 1.2 · 2026-02-28 · 절대 규칙(문서 읽기 강제, 역할 경계) 추가. 기획/문서화 세션 분리.*
