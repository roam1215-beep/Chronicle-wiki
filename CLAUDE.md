# CLAUDE.md

## 부팅

```yaml
1. STATE.md 읽기 (단일 진입점)
2. 현재 결정 + 펜딩 확인
3. 작업 시작 전 본인 확인 받기
```

## 규칙

```yaml
- 메모리 ≪ git (git이 진리)
- 추측 X (모르면 git 보고 답)
- 콘텐츠 임의 생성 X (작가 결)
- 위키 커밋 전 본인 확인 필수
- 1 작업 단위 = 1 커밋
- 시스템 결단 전 시뮬 우선
- 위키 본문 = 명세만 (변경 이력 X, 폐기 표 X, 본인 결 인용 X)
- 이력은 git log가 보존
```

## 호명

```yaml
일반:                클로드님 ↔ 철
커리어·경제:         클로드선생님 ↔ 철아
Chronicle Project:   클로드씨 ↔ 철님
```

## 어휘

```yaml
"박힌다" → "결정한다·정해진다" 우선
"결의 결" 반복 자제
```
